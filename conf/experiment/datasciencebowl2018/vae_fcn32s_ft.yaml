# @package _global_

defaults:
 - datasciencebowl2018/common
 - /common/model: fcn32s

model:
  out_channels: 3
  pretrained: last://last    # best://[dice, jaccard, loss]
  reset_clf: 1
  latent_sampling: True