# @package _global_

defaults:
 - brainMRI/common
 - /common/model: hunet2

model:
  hebb:
    unit_type: 'DotUnit'
    w_nrm: True
    mode: 'SoftWinnerTakesAll'
    alpha: 0
  pretrained: dice    # [dice, jaccard, loss]

optim:
  batch_size: 4
  batch_accumulation: 4

