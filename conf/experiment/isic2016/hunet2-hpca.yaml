# @package _global_

defaults:
 - isic2016/common
 - /common/model: hunet2

model:
  hebb:
    unit_type: 'DotUnit'
    w_nrm: False
    mode: 'HebbianPCA'
    alpha: 1

optim:
  batch_size: 4
  batch_accumulation: 4