# @package _global_

defaults:
 - ph2/common
 - /common/model: unet

model:
  out_channels: 3
  pretrained: last://last    # best://[dice, jaccard, loss]
  reset_clf: 1
  latent_sampling: True

optim:
  entropy_lambda: 0.005
