# @package _global_

defaults:
 - ph2/common
 - /common/model: hunet

model:
  hebb:
    w_nrm: False
    mode: 'hpca'
    patchwise: True
    contrast: 1.
    uniformity: False
    alpha: 0
  pretrained: dice    # [dice, jaccard, loss]

optim:
  batch_size: 4
  batch_accumulation: 4

