# @package _global_

defaults:
 - ph2/common
 - /common/model: unet

optim:
  epochs: 300

  optimizer:
    lr: 0.001

  lr_scheduler:
      milestones: [100, 150, 200]

  wavelet_lambda: adaptive
  starting_wavelet_lambda: 1