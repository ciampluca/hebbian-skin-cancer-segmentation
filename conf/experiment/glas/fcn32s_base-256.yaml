# @package _global_

defaults:
 - glas/common
 - /common/model: fcn32s

optim:
  optimizer:
    lr: 0.001

  epochs: 400

  lr_scheduler:
      milestones: [350, 380, 400]

data:
  image_size: 256
  train:
    use_pseudolabel: false