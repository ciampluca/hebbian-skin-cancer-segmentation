# @package _global_

defaults:
 - drive/common
 - /common/model: hunet2

model:
  hebb:
    unit_type: 'DotUnit'
    w_nrm: False
    mode: 'HebbianPCA'
    alpha: 0
  pretrained: last://last    # best://[dice, jaccard, loss]
  reset_clf: true

optim:
  batch_size: 4
  batch_accumulation: 4

