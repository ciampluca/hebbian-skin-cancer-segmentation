# @package _global_

defaults:
 - eyes/common
 - /common/model: unet

optim:
  epochs: 300

  optimizer:
    lr: 0.0001

  lr_scheduler:
      milestones: [100, 200, 250]

  wavelet_lambda: adaptive
  starting_wavelet_lambda: 3